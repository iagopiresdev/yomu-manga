/*

import { Router } from 'express';
import * as PostController from '../controllers/postController';

const router = Router();

router.get('/:mangaId', PostController.getAll); // Get all posts for a manga
router.get('/:mangaId/:postId', PostController.getOne); // Get a specific post for a manga by postId
router.post('/:mangaId', PostController.create); // Create a new post for a manga
router.put('/:mangaId/:postId', PostController.update); // Update a post for a manga by postId
router.delete('/:mangaId/:postId', PostController.delete); // Delete a post for a manga by postId

export default router;

*/